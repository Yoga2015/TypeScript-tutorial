import axios from 'axios'
import _ from 'lodash'

axios({
  url: '',
  method: 'GET'
})


// 内置的类型声明文件

let arr3 = [1, 2, 3, 4, 5]

arr3.forEach

document.querySelector

window

// 第三方库的类型声明文件
_.forEach